# exam_training 每日学习操作指南

> 当前阶段：知识恢复强化期
>
> 核心方法：旧知识先测 → 定点补 → 再测；读题拆解 → 方法调用 → 计算

## 每天

### 早上 / 公交

直接打开：

```text
TODAY.md
```

按完整公交包执行，不需要每做5题就切回Codex。

### 训练中

正常告诉Codex答题结果即可，例如：

```text
第1组5题，对4题；能口述方法；第3题不确定。
```

Codex会保留已知信息。已经写入 `TODAY.md`、日志、CSV或当前对话的数据，不会再次要求填写。

### 训练结束

只需要发送：

```text
训练结算
```

Codex自动执行：

```text
读取TODAY和已有报告
→ 只询问缺失数据
→ 填写TODAY训练结果
→ 写入logs
→ 更新stats
→ 必要时更新wrongbook
→ 判断知识点是否推进
→ 根据情况生成次日计划
→ Git commit
→ Git pull --rebase
→ Git push
```

用户不需要手工填写日志、CSV、整理正确率，也不需要向ChatGPT重复汇报已经推送到GitHub的信息。

## 三种训练状态

- A日：60～75分钟，一个主知识点 + 一个轻量副模块，通勤计入总量。
- B日：35～45分钟，一个主项、一至两组小题、一个极轻副任务。
- C日：15～20分钟，方法回忆 + 3～5题 + 口述。
- 周末有完整时间：90～120分钟分两块；有生活安排可休息，不补课。

状态变化时只需发送：

```text
改B日
```

或：

```text
按C日
```

降级不等于失败。当天未完成任务不补课、不顺延、不产生训练债务。

## 完全不会的题

发送：

```text
先别给答案。请从knowledge定位题型和缺失知识，只补完成这道题需要的最小内容，然后让我自己重做。
```

Codex只会从 `knowledge/00_总索引.md` 定位当前主项/副项所需精确小节，不扫描整个知识库。

## 知识点推进

只有同时满足以下条件才进入下一知识点：

1. 最近连续两次同知识点专项均≥80%，即5题至少对4题。
2. 能脱离资料口述识别信号、基本方法/公式和一个高频陷阱。

数据没有明确报告时不推定达标。继续未达标知识点是正常推进，不是补课。

## GitHub与ChatGPT监督

训练记录只有完成以下链路，才算同步：

```text
文件更新 → commit → push成功
```

ChatGPT从GitHub读取 `TODAY.md`、`CURRENT_WEEK.md`、`logs/` 和 `stats/training.csv`。因此，已经成功push的数据不需要再次向ChatGPT汇报。

如果push失败，Codex会明确说明本地已保存、GitHub尚未同步以及具体原因，不会假装同步成功。

## 最简操作

```text
早上：打开TODAY执行
训练中：正常报告答题结果
结束：发送“训练结算”
其余记录、统计和Git同步由Codex完成
```
## 恢复与强化

旧知识诊断前不看公式：≥4/5且能口述直接第二组；2～3/5或方法不全只补缺口；≤1/5或核心方法完全遗忘，先学该点和例题再5题。
计算前说明时间、已知、所求、题型、公式及陷阱。会从母公式推导也算方法基本掌握。
资料、判断、言语并行；某项未达标留到下次该模块。申论每周至少2次、每次20～30分钟。已掌握项后续复习转10～15题计时。
9月30日检查恢复程度，10月逐步转混合题、限时和真题。

## 核心任务与工作空闲

每天先完成TODAY核心任务，A/B/C时长为核心预算（含通勤）。工作特别闲也不扩张必做量。核心完成后可以结束，机动池只是候选，不做或中断不影响当天完成状态。

- 说“继续”：Codex核对已完成内容、剩余池和机动用时，只下发下一块；结束后再次明确继续才给下一块。
- 说“现在有20分钟空闲”：按真实弱项及剩余模块选一块15～20分钟训练。
- 说“工作来了，暂停”：保存实际作答和用时，余下取消，不要求晚上补；做3题就记3题。

每块10～30分钟，工作日额外累计建议约60分钟；不要求学满。到上限先提醒继续属于额外自愿训练，仅明确要求才继续。未执行块当天作废、不顺延、不透支明天。

优先已学验证、调用或复习，再其他模块诊断和轻申论；有待闭卷验证的知识点时避免提前练同类新题。全天原则上最多正式开启一个新主知识点。诊断仅测基础，不能直接认定恢复；日志区分诊断、带提示训练、正式专项、闭卷验证、计时专项。

最终“训练结算”合并核心与机动实际成绩，CSV照常按模块累计，日志逐块记录；ChatGPT晚间监督分别看核心完成、机动量及次日疲劳/启动。今天9月14日候选：翻译推理5题诊断→言语5～10题诊断→申论关键词提炼；保护3.5闭卷验证，9月15日休息。
